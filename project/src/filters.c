#include "filters.h"

// TODO(hw1): Replace this stub with Lab 1 low/high-pass and moving-average filters.
void filters_placeholder_init(void) {
    // Intentionally empty until filter implementations are migrated.
}
